# 🚀 离线部署指南

本项目支持**完全离线部署**，除了 Whisper 模型外，所有依赖都可以一次性部署。

## 📋 部署清单

### 1. Python 依赖（自动安装）
所有 Python 库都在 `config/requirements.txt` 中列出，通过以下命令一次性安装：

```bash
pip install -r config/requirements.txt
```

包含的库：
- **Flask** - Web 框架
- **Flask-CORS** - 跨域资源共享
- **openai-whisper** - 语音识别（可选，若无GPU则使用CPU）
- **PyPDF2** - PDF 处理
- **ebooklib** - EPUB 处理
- **python-docx** - Word 文档处理
- **torch** - 深度学习框架（Whisper 依赖）
- **numpy** - 数值计算库

### 2. 前端库（已本地化）
所有 JavaScript 库都已下载到 `static/lib/` 目录：

```
static/lib/
├── pdf.min.js                  # PDF.js 核心库
├── pdf.worker.min.js           # PDF.js Worker 线程
├── pdf_viewer.min.css          # PDF 查看器样式
├── wavesurfer.min.js           # 波形图库
├── regions.min.js              # 波形区域选择插件
├── marked.umd.js               # Markdown 解析
├── mammoth.browser.min.js      # Word 文档处理
└── epub.min.js                 # EPUB 电子书处理
```

**大小统计：**
| 库 | 大小 |
|----|------|
| pdf.worker.min.js | 1.04 MB |
| mammoth.browser.min.js | 628 KB |
| epub.min.js | 225 KB |
| pdf.min.js | 312 KB |
| marked.umd.js | 97 KB |
| regions.min.js | 15 KB |
| wavesurfer.min.js | 39 KB |
| pdf_viewer.min.css | 43 KB |
| **总计** | **2.4 MB** |

### 3. Whisper 模型（可选，按需下载）
**注意：** 模型文件较大（39MB-2.9GB），不包含在本项目中。

首次运行时，应用会自动下载选定的模型。可选的模型：

| 模型 | 大小 | 准确度 | 速度 |
|------|------|--------|------|
| tiny | 39 MB | ⭐ | 🚀🚀🚀 |
| base | 140 MB | ⭐⭐ | 🚀🚀 |
| small | 466 MB | ⭐⭐⭐ | 🚀 |
| medium | 1.5 GB | ⭐⭐⭐⭐ | 📊 |
| large | 2.9 GB | ⭐⭐⭐⭐⭐ | 📊 |

**建议配置：**
- 无 GPU：选择 tiny 或 base
- 有 GPU：选择 medium 或 large

---

## 🔧 完整部署步骤

### 步骤 1：环境准备
```bash
# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境
# Windows
.venv\Scripts\activate
# macOS/Linux
source .venv/bin/activate
```

### 步骤 2：安装依赖
```bash
# 所有依赖已列出，一次性安装
pip install -r config/requirements.txt

# 如果需要 GPU 加速，额外安装
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### 步骤 3：运行应用
```bash
# 启动应用服务器
python start.py

# 或直接运行
cd src
python app.py
```

### 步骤 4：访问应用
打开浏览器访问：`http://localhost:5000`

---

## 📦 离线部署要求

### 必需文件
```
RussianLeraning/
├── static/
│   ├── lib/              # ✅ 前端库（已包含）
│   ├── app.js
│   ├── index.html
│   ├── pdf-viewer.html
│   └── styles.css
├── config/
│   └── requirements.txt   # ✅ Python 依赖列表
├── src/
│   └── app.py
└── start.py
```

### 可选文件
```
models/                    # Whisper 模型（首次运行时自动下载）
user_data/                # 用户数据（自动生成）
```

---

## ✅ 验证部署

运行以下命令验证所有依赖都已正确安装：

```bash
# 检查 Python 库
pip list | grep -E "Flask|whisper|PyPDF2|ebooklib"

# 检查 JavaScript 库
ls -la static/lib/

# 检查应用是否可启动
python start.py
```

---

## 🌐 在线 vs 离线

### 在线安装
- ✅ 自动下载最新版本
- ✅ 自动解决依赖冲突
- ❌ 需要网络连接
- ❌ 安装速度取决于网络

### 离线安装（本项目）
- ✅ 无需网络连接
- ✅ 快速部署
- ✅ 版本固定，稳定可靠
- ✅ 所有资源预先打包
- ❌ 需要提前下载（仅首次）

---

## 🐛 故障排除

### 问题：缺少前端库
**症状：** 页面加载不完整，控制台 404 错误

**解决方案：** 确保 `static/lib/` 目录存在所有文件
```bash
ls static/lib/
# 应该显示 8 个文件
```

### 问题：Python 依赖冲突
**症状：** `pip install` 失败

**解决方案：** 清理虚拟环境并重新安装
```bash
deactivate
rm -rf .venv
python -m venv .venv
.venv\Scripts\activate
pip install -r config/requirements.txt
```

### 问题：Whisper 模型下载失败
**症状：** 首次使用语音识别时出错

**解决方案：** 手动下载模型
1. 访问 https://openaipublic.azureedge.net/
2. 下载所需模型到 `models/` 目录
3. 重启应用

---

## 📊 部署架构

```
┌─────────────────────────────────────────┐
│         用户浏览器 (客户端)              │
├─────────────────────────────────────────┤
│  HTML + CSS + JavaScript (本地化)       │
│  - 无 CDN 依赖                          │
│  - 所有库在 static/lib/ 中              │
└─────────────────────────────────────────┘
              ↕ HTTP
┌─────────────────────────────────────────┐
│       Flask 应用服务器 (后端)            │
├─────────────────────────────────────────┤
│  - 文档处理 (PyPDF2, ebooklib)          │
│  - 语音识别 (Whisper)                   │
│  - 数据管理 (用户数据)                   │
└─────────────────────────────────────────┘
              ↕ 本地存储
┌─────────────────────────────────────────┐
│         本地文件系统 (数据)               │
├─────────────────────────────────────────┤
│  user_data/ - 用户数据                  │
│  models/    - Whisper 模型              │
└─────────────────────────────────────────┘
```

---

## 🎯 关键点总结

1. **无 CDN 依赖** - 所有前端库本地化，无需网络
2. **一键安装** - `pip install -r config/requirements.txt`
3. **模型可选** - Whisper 模型按需下载
4. **离线优先** - 除了模型外，完全离线可用
5. **生产就绪** - 所有版本固定，稳定可靠

---

**现在您可以在完全离线环境中部署本项目！** 🚀
